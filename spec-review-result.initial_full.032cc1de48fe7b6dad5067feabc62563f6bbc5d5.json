{
  "result": "fail",
  "findings": [
    {
      "severity": "P2",
      "summary": "The job executes sources sequentially using await inside a for...of loop instead of concurrently, and the test asserting concurrency is flawed.",
      "file": "src/jobs/on-chain-flow-job.ts",
      "suggested_fix": "Use Promise.all(sortedSources.map(...)) to start all source collections concurrently rather than awaiting each one in a sequential loop."
    }
  ]
}
