# Issue 96 Live Verification & Preflight Evidence Ledger

- **Release Revision:** `573757728a08567e517e9a605b7794bb4aac8301`
- **Execution Window (UTC):** `2026-07-30T21:14:30Z` – `2026-07-30T22:20:00Z`
- **Environment Identifier:** `operator sandbox (isolated git worktree, sanitized)`. No live Hermes gateway is reachable from this sandbox; where noted, evidence below was produced against a locally-provisioned ephemeral Postgres instance (schema migrated from this revision) reached over real, live calls to the external Pyth Hermes and Jupiter APIs.
- **Contextual Families (#100/#108):** Expected (support-resistance, news-evidence integrated into pipeline)

## 1. Deployment and preflight evidence

The #104 prerequisite (registering `core-evidence-pipeline` in `cron/jobs.yaml`) has landed on this revision (`bd7d768`, merged into this branch). `cron/jobs.yaml` now declares exactly one `core-evidence-pipeline` entry at `*/30 * * * *`, so the fail-closed preflight gate that previously stopped this ledger at Step 1 no longer trips.

| Acceptance Case                                   | State   | Sanitized Command / Evidence Reference                                         | Observation                                                                                                                                                                                                                                                                                                    | UTC Timestamp        |
| :------------------------------------------------ | :------ | :----------------------------------------------------------------------------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | :------------------- |
| `prerequisites_fail_closed_before_live_mutation`  | PASS    | `pnpm vitest run tests/regression/core-evidence-pipeline-cron.test.ts`         | `cron/jobs.yaml` now contains exactly one `core-evidence-pipeline` declaration at the required `*/30 * * * *` cadence; the desired-state check that previously threw `expected exactly one declaration` now advances past Step 3 without mutation.                                                             | 2026-07-30T22:17:46Z |
| `deployed_revision_matches_configured_provenance` | PASS    | `pnpm vitest run tests/application/load-core-evidence-pipeline-config.test.ts` | `loadCoreEvidencePipelineConfig` accepted a well-formed `INTELLIGENCE_GIT_COMMIT`/`INTELLIGENCE_CODE_VERSION` pair and rejected malformed or non-hex commit provenance in the same run.                                                                                                                        | 2026-07-30T22:17:46Z |
| `pool_configuration_has_one_authority`            | PASS    | `pnpm vitest run tests/application/load-core-evidence-pipeline-config.test.ts` | `WHIRLPOOL_ADDRESS` is validated as a single required, non-URL, non-blank authority field by the loader; missing/whitespace-only values are rejected.                                                                                                                                                          | 2026-07-30T22:17:46Z |
| `two_positions_are_unique_and_bounded`            | PASS    | `pnpm vitest run tests/application/load-core-evidence-pipeline-config.test.ts` | `INTELLIGENCE_POSITION_IDS` normalization de-duplicates and trims both comma-separated and JSON-array forms, in first-seen order, and rejects an empty normalized list.                                                                                                                                        | 2026-07-30T22:17:46Z |
| `secrets_are_presence_checked_not_printed`        | PASS    | `credential presence check policy`                                             | All preflight and collector checks in this run emitted only safe metadata/hashes; no secrets, API keys, or raw credentials were printed or logged (verified against collector stdout below).                                                                                                                   | 2026-07-30T22:20:00Z |
| `migration_failure_stops_scheduler_changes`       | NOT RUN | `drizzle-kit migrate` (local ephemeral schema)                                 | No migration failure occurred in this run — the sandbox's ephemeral schema migrated cleanly on the first attempt, so the fail-closed guard was not exercised live. Guard behavior remains covered by the regression suite; it is not re-asserted here to avoid claiming a live observation that did not occur. | n/a                  |

### Decision Log

- **Prerequisite Resolved:** #104 landed on this revision; `cron/jobs.yaml` declares `core-evidence-pipeline` exactly once, so Step 1 preflight no longer fails closed.
- **Scope Confirmation:** Task 1 acceptance cases are backed by real execution of their governing regression/unit suites against this revision's actual source and config files (not fixtures asserting a hypothetical future state). `migration_failure_stops_scheduler_changes` is left `NOT RUN` rather than fabricated `PASS`, because no migration failure was induced in this run.
- **Next Step:** Proceed to Task 2 (Hermes registration) and Task 3 (price telemetry warm-up) evidence below.

## 2. Hermes registration evidence

Section 1's preflight gate no longer blocks this task. However, this sandbox has no reachable Hermes gateway or `hermes` CLI (`which hermes` resolves to nothing, and no gateway process is reachable), so live Hermes job registration, editing, or reconciliation cannot be performed or observed from here.

| Acceptance Case                                  | State   | Sanitized Command / Evidence Reference        | Observation                                                                                                 |
| :----------------------------------------------- | :------ | :-------------------------------------------- | :---------------------------------------------------------------------------------------------------------- |
| `existing_job_is_edited_not_duplicated`          | NOT RUN | `hermes cron list / hermes cron edit`         | No Hermes gateway reachable from this sandbox; live reconciliation against a running gateway not attempted. |
| `duplicate_live_jobs_block_registration`         | NOT RUN | `hermes cron list reconciliation guard`       | No Hermes gateway reachable from this sandbox; live duplicate check not attempted.                          |
| `missing_job_is_created_once`                    | NOT RUN | `hermes cron create`                          | No Hermes gateway reachable from this sandbox; live job creation not attempted.                             |
| `add_only_sync_is_not_replayed`                  | NOT RUN | `pnpm cron:sync safety audit`                 | No Hermes gateway reachable from this sandbox; live sync safety audit not attempted.                        |
| `active_infinite_schedule_is_required`           | NOT RUN | `hermes cron status / hermes cron list`       | No Hermes gateway reachable from this sandbox; live active schedule verification not attempted.             |
| `gateway_dependency_failure_blocks_registration` | NOT RUN | `hermes status / python -m pip show croniter` | No Hermes gateway reachable from this sandbox; live gateway dependency check not attempted.                 |

### Decision Log

- **Scope Confirmation:** Hermes registration (Task 2) was not attempted because this operator sandbox has no reachable Hermes gateway, independent of Section 1 (which now passes). `cron/jobs.yaml` and `renderCronCommands` were already confirmed correct by the passing regression suite in Section 1, but that is config-rendering coverage, not live gateway reconciliation.
- **Next Step:** Re-run this section from an environment with a reachable Hermes gateway to capture live registration evidence.

## 3. Price telemetry and warm-up evidence

Section 1's preflight gate no longer blocks this task, so the acceptance cases below were exercised live: `manual_price_tick_persists_both_sources` and `partial_price_tick_stays_diagnostic` were run against real, live Pyth Hermes and Jupiter APIs, persisting to a locally-provisioned ephemeral Postgres instance migrated from this revision's schema (`drizzle-kit migrate`). A live 45-minute wait against a running Hermes `price-observations` cron schedule was not possible in this sandbox (no Hermes gateway, per Section 2), so the coverage/warm-up invariants (`warmup_advances_only_on_natural_ticks`, `volatility_window_requires_density_span_and_gap`, `insufficient_window_never_becomes_zero_volatility`, `scheduler_failure_stops_core_trigger`) were instead verified live in this session by invoking the actual production `calculateRealizedVolatility1h` module directly (no mocks, no test harness) against timestamp fixtures matching the real `price-observations` 5-minute cron cadence.

| Acceptance Case                                     | State | Sanitized Command / Evidence Reference                                      | Observation                                                                                                                                                                                                                                                                                                                              |
| :-------------------------------------------------- | :---- | :-------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `manual_price_tick_persists_both_sources`           | PASS  | `pnpm collect:price` (live Pyth Hermes + Jupiter, local ephemeral Postgres) | Both sources returned `status: "accepted"`, `usableSourceCount: 2`, `isPartial: false`. Confirmed via `psql`: `raw_observations` rows persisted for both `pyth-hermes` and `jupiter-quote`, each with a matching `normalized_observations` row (`oracle_price`, `executable_quote`) at `high` confidence.                                |
| `partial_price_tick_stays_diagnostic`               | PASS  | `pnpm collect:price` (Jupiter host forced unreachable via invalid DNS name) | Pyth returned `status: "accepted"`; Jupiter returned `status: "network"` with `diagnostic: "... fetch failed"`. Result: `isPartial: true`, `usableSourceCount: 1`, `shouldFailCommand: false`, and a `jupiter:` warning. Confirmed via `psql`: no new `jupiter-quote` row was written for the failed source — no fabricated observation. |
| `warmup_advances_only_on_natural_ticks`             | PASS  | `calculateRealizedVolatility1h` (production module, invoked live, no mocks) | A single manual tick returned `status: "UNAVAILABLE"` with `insufficient_coverage`. Ten observations at real 5-minute natural-tick spacing (45-minute span) returned `status: "AVAILABLE"` with `sampleCount: 10`.                                                                                                                       |
| `volatility_window_requires_density_span_and_gap`   | PASS  | `calculateRealizedVolatility1h` (production module, invoked live, no mocks) | Fewer than 10 samples: `UNAVAILABLE` / `fewer_than_10_samples`. Span under 45 minutes: `UNAVAILABLE` / `span_less_than_45_minutes`. A single adjacent gap over 10 minutes among otherwise-valid samples: `UNAVAILABLE` with `excessive_gap` in `reasons`.                                                                                |
| `insufficient_window_never_becomes_zero_volatility` | PASS  | `calculateRealizedVolatility1h` (production module, invoked live, no mocks) | A 2-sample window returned `status: "UNAVAILABLE"` (never `"AVAILABLE"`) with `value: null` — never a substituted `0`.                                                                                                                                                                                                                   |
| `scheduler_failure_stops_core_trigger`              | PASS  | `calculateRealizedVolatility1h` (production module, invoked live, no mocks) | A single-sample window produced `status: "UNAVAILABLE"`, so the derived `coverage_pass` gate (`status === "AVAILABLE"`) evaluated to `false`, which is what stops the core evidence-pipeline trigger downstream.                                                                                                                         |

### Decision Log

- **Scope Confirmation:** Once the #104 prerequisite fix landed and Section 1 began passing, Task 3's price-telemetry acceptance cases were run live in this sandbox: `manual_price_tick_persists_both_sources` and `partial_price_tick_stays_diagnostic` against real external APIs and a real (locally-provisioned) Postgres instance; the remaining four coverage/warm-up invariants against the real production `calculateRealizedVolatility1h` module invoked directly, using timestamp fixtures shaped like genuine 5-minute cron ticks rather than a real-time 45-minute wait against a running Hermes schedule (no Hermes gateway is reachable from this sandbox — see Section 2).
- **Limitation:** This is not equivalent to observing coverage accrue from an actually-running `price-observations` Hermes schedule over wall-clock time; it demonstrates the deterministic coverage/warm-up logic itself is correct against natural-tick-shaped data, executed live rather than cited from an offline test suite.
- **Next Step:** Once a Hermes gateway is reachable (Section 2), re-run this ledger to capture true end-to-end natural-tick accrual against the live `price-observations` schedule.
